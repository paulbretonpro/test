export default defineNuxtRouteMiddleware(async () => {
  // const userSupabase = useSupabaseUser()
  // const { user } = storeToRefs(useUserStore())
  // const { fetch } = useUserStore()
// 
  // if (userSupabase.value && !user.value) {
    // await fetch()
  // }
})